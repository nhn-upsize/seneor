# 작업 규칙

이 저장소에서 분기 보고서를 만들거나 고치기 전에
**[`docs/QUARTERLY.md`](docs/QUARTERLY.md) 를 먼저 읽으세요.** 아래는 그중 절대 어기면 안 되는 것만 추린 것입니다.

## 새 분기는 "새로 만드는" 게 아니라 "기간을 연장하는" 것

프레임(탭 4개·국가별 섹션 ①~⑥·차트 종류)은 그대로 두고 기간만 늘립니다.
`report_pipeline/config.py` 의 PERIOD 블록만 수정하고 다른 파일은 건드리지 마세요.

## 집계 기준

- OS 통합 (`in_revenue_top100_unified_os = TRUE`, `unified_app_id` 로 중복 제거)
- 매출 = `revenue_krw_100` (환율 반영 완료 — 다시 계산하지 말 것)
- 월평균 분모 = **달력 개월수 고정** (활동월 수 아님)
- 연평균 = 월별 SUM 후 평균 (`GROUP BY name` + `AVG` 는 OS 이중 카운트로 50% 오류)
- 국적: **NEXON → KR**, **FUNFLY → 중화권** 강제
- **중화권 = China · Hong Kong · Taiwan · Macao 만. 싱가포르는 절대 포함하지 않음**
  (FUNFLY 하나만 예외. 2026-07에 이걸 어겼다가 전부 원복한 이력이 있음)
- 차트 항목 선정 = Q2매출 상위5 + 증감 상위3씩, **절대금액 기준** (퍼센트 기준 금지)

## 고치기 전에

- **기존 값이 틀렸다고 단정하지 말 것.** DB와 다르면 먼저 기존 값의 근거를 확인하세요.
  과거에 "수정"했다가 기존 값이 맞아서 3국 전부 되돌린 적이 있습니다
- 규칙이 헷갈린다는 지적을 받아도 **규칙을 바꾸지 말고 라벨로 설명**하세요
  (부분 분기 게임 → `(6월신규,1개월)` 식 표기). 2026-07 사용자 결정입니다
- 수치 하나를 바꾸면 같은 섹션의 **본문·요점·헤드라인·차트 라벨까지** 함께 고치세요.
  같은 숫자가 5~6곳에 복제돼 있습니다

## 발행 전 필수

```bash
python -m report_pipeline.validate    # ✅ 0건 될 때까지
```

추가로 손으로 확인: 상단 요약박스 ↔ 하위 섹션 수치 대조, hbar 차트와 trend 라인차트의
항목 개수 일치.

## 이름·주소 규칙

`26Y3Q/` 폴더 · 본편 `index.html` · 요약본 `summary.html` ·
문서명 `26년 3분기 시장 분석 보고서` / `26년 3분기 요약본`.
새 분기를 추가하면 **루트 `index.html` 의 리다이렉트 경로**를 새 폴더로 바꿔야 합니다.
자세한 건 `README.md` 참조.

## 데이터 취급

`data/` `output/` `reports/` `*.csv` `*.xlsx` 는 **Sensor Tower 라이선스 데이터**라 커밋하지 않습니다.
접속 정보는 `.env` 로만 다루고 코드에 넣지 마세요.
